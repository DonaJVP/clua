#pragma once

// Should it show what code is generated inside compiler?
static bool showGeneratedCode = false;

// Should export generated raw code into a file?
static bool exportCodeToUniqueFile = false;
static char* _ECTUF_Filename = nullptr;
